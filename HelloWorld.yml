---
# This playbook prints a simple debug message
- name: Echo
  hosts:
    - dev.inv
  become: yes
  become_user: root
  become_method: su
  gather_facts: false

  tasks:
  - name: Print debug message
    debug:
      msg: Hello, world!
